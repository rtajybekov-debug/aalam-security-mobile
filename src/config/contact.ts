/** Single source of truth for the dispatch / support contact number. */
export const SUPPORT_PHONE = {
  /** E.164 form — used for `tel:` links. */
  e164: "+996505022891",
  /** Human-readable form — used in UI copy. */
  display: "+996 505 022 891",
} as const;
